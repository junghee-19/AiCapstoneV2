services:
  edge:
    build:
      context: .
      dockerfile: edge/Dockerfile
    container_name: inspection-edge
    env_file:
      - path: ./edge/.env
        required: false
    environment:
      TZ: Asia/Seoul
    ports:
      - "${EDGE_API_PORT:-8000}:8000"
    devices:
      # Override CAMERA_DEVICE from the shell or project-root .env when the camera is not /dev/video0.
      - "${CAMERA_DEVICE:-/dev/video0}:${CAMERA_DEVICE:-/dev/video0}"
    volumes:
      - ./edge/weights:/app/weights
      - ./edge/captures:/app/captures
      - ./edge/demo_samples:/app/demo_samples
    restart: unless-stopped
